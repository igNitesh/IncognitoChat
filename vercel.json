{
  "version": 2,
   "builds": [{
        "src": "mark2/asgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "mark2/asgi.py"
        }
    ]
}
